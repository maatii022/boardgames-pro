services:
  - type: web
    name: feed-the-kraken
    runtime: node
    rootDir: servidor
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
